import { BrowserListenerDirective } from './browser-listener.directive';

describe('BrowserListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new BrowserListenerDirective();
    expect(directive).toBeTruthy();
  });
});
