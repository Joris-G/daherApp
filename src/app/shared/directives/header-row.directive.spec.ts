import { HeaderRowDirective } from './header-row.directive';

describe('HeaderRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderRowDirective();
    expect(directive).toBeTruthy();
  });
});
