import { DataRowDirective } from './data-row.directive';

describe('DataRowDirective', () => {
  it('should create an instance', () => {
    const directive = new DataRowDirective();
    expect(directive).toBeTruthy();
  });
});
