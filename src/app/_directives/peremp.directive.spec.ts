import { PerempDirective } from './peremp.directive';

describe('PerempDirective', () => {
  it('should create an instance', () => {
    const directive = new PerempDirective();
    expect(directive).toBeTruthy();
  });
});
